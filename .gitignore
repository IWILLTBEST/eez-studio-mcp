__pycache__/
*.pyc
_shots/
node_modules/
*.vsix
*.eez-extension
golden/*.last.png
golden/*.diff.png

# strays from manual `-o` compiles outside the build/ convention
*.eez-project
*.eez-project-ui-state
action.h

# ALL generated output lives in examples/<name>/build/ — project, C export,
# side-cars and the simulator. Rebuild with tools/build_sim.py / ci-check.py.
# 产物统一收进 build/：工程、C 导出、伴生文件与模拟器。
examples/*/build/
.sim-cache/
